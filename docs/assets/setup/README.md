# Setup storyboards (SVG)

Vector figures for [docs/manual/01-setup.md](../../manual/01-setup.md): **BotFather** chat (non-CRT), **provider auth** and **first-bot wizard** (CRT glow, same family as [`../octopus/`](../octopus/) and [`../registry/`](../registry/)).

PNG fixtures under `docs/assets/manual/setup-*` remain for the Playwright capture pipeline (`capture-manual.spec.ts`) if you regenerate annotated raster assets; the **manual** prefers these SVGs for crisp scaling and consistent styling.
